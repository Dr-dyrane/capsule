# Archived Capsule Migrations

These files are historical rollout steps that are no longer part of the active
Supabase migration chain.

They were archived because their changes are now folded into the small core
migration set under `supabase/migrations/`, or because they were one-time
backfills that should not run on a fresh reset.

Nothing in this folder should be executed automatically.
